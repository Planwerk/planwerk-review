Several code-review passes produced the findings below. Some may describe the SAME underlying issue in different words. Group the findings that are duplicates of one another.

Output ONLY valid JSON matching this exact schema (no markdown fences, no surrounding text):

{
  "duplicate_groups": [[0, 3], [1, 5]]
}

Each inner array lists the 0-based indices of findings that describe the same issue. Rules:
- Group ONLY findings that are genuinely the same issue — same defect, same location, or same root cause.
- Every index appears in at most one group, and a group has at least two indices.
- Do NOT group findings that merely touch the same file or area but describe different problems.
- An empty "duplicate_groups" array is the correct answer when nothing is duplicated. Do NOT invent groups.

<findings>
0. [CRITICAL] Race on shared counter — (no file)
   The counter is incremented without a lock.
1. [CRITICAL] Data race incrementing the counter — worker.go:12
   Concurrent increments corrupt the count.
</findings>